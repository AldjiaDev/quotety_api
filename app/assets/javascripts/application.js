// Import des dépendances Rails de base
import Rails from "@rails/ujs"
Rails.start()

// Import de Turbo (si tu utilises Turbo dans Rails 7)
import "@hotwired/turbo-rails"
import "controllers"

// Import des contrôleurs Stimulus si tu en as
import "controllers"

// Import de Bootstrap (si nécessaire)
import "bootstrap"
