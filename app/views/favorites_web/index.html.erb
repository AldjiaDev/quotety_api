<div class="container py-5">
  <h2 class="fw-bold text-center mb-4">❤️ Vos citations favorites</h2>

  <% if @favorites.empty? %>
    <p class="text-center text-muted">Vous n'avez encore ajouté aucune citation à vos favoris.</p>
  <% else %>
    <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-4">
      <% @favorites.each do |quote| %>
        <div class="col">
          <div class="card shadow-sm border-0 h-100" style="border-radius: 1rem;">
            <div class="card-body d-flex flex-column justify-content-between p-4" style="height: 250px;">
              <blockquote class="blockquote mb-3">
                <p class="mb-0 fst-italic">“<%= quote.content %>”</p>
              </blockquote>
              <footer class="blockquote-footer text-muted">
                <%= quote.author.name %>
              </footer>
            </div>
            <div class="card-footer bg-white border-0 d-flex justify-content-between align-items-center px-4 pb-3">
              <%= button_to "❌", remove_favorite_path(quote.id), method: :delete, class: "btn btn-sm btn-outline-danger", data: { confirm: "Supprimer cette citation de vos favoris ?" } %>
              <a href="#" class="btn btn-sm btn-outline-secondary">🖨 Imprimer</a>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
