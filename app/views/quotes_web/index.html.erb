<!-- HERO SECTION -->
<div class="hero-banner text-white d-flex align-items-center" style="background-image: url('<%= asset_path("hero.jpg") %>'); height: 450px; background-size: cover; background-position: center;">
  <div class="container z-2">
    <div class="row">
      <div class="col-md-6">
        <h1 class="display-4 fw-bold">Trouvez l'inspiration.<br><span class="text-gradient">Une citation à la fois.</span></h1>
        <p class="lead">Explorez des pensées puissantes, partagez des mots qui touchent l'âme, imprimez ce qui vous élève.</p>
        <div class="d-flex gap-2">
          <%= link_to "Explorer les citations", quotes_path, class: "btn btn-primary" %>
          <%= link_to "Citation aléatoire", "#", class: "btn btn-outline-primary" %>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- CATEGORIES -->
<div class="bg-light py-5">
  <div class="container">
    <div class="text-center mb-5">
      <h2 class="fw-bold fs-3">💬 Explorez les <strong>catégories</strong></h2>
      <p class="text-muted">Des citations inspirantes pour chaque univers.</p>
    </div>

    <div class="row">
      <% categories = ["amour", "humour", "litterature", "motivation", "sagesse", "vie"] %>
      <% categories.each do |category| %>
        <div class="col-md-4 mb-4">
          <div class="card h-100">
            <%= image_tag "categories/#{category}.jpg", class: "card-img-top", alt: category.capitalize %>
            <div class="card-body text-center">
              <h5 class="card-title"><%= link_to category.capitalize, category_quotes_path(category: category), class: "stretched-link text-decoration-none" %></h5>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>

<!-- ALL QUOTES -->
<div class="container py-5">
  <div class="text-center mb-5">
    <h2 class="fw-bold fs-3">📚 Toutes les <strong>citations</strong></h2>
    <p class="text-muted">Laissez-vous inspirer par les mots des grands auteurs.</p>
  </div>

  <div class="row">
    <% @quotes.each do |quote| %>
      <div class="col-md-6 mb-4">
        <div class="card shadow-sm h-100">
          <div class="card-body">
            <blockquote class="blockquote mb-3">
              <p class="mb-0">« <%= quote.content %> »</p>
            </blockquote>
            <footer class="blockquote-footer">
              <%= quote.author.name %>
              <% if quote.category %> — <%= quote.category.name %><% end %>
            </footer>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>

<!-- FOOTER -->
<footer class="py-4 mt-5" style="background-color: #3b2f2a; color: #fef5ed;">
  <div class="container text-center">
    <p class="mb-0">&copy; 2025 <strong>Quotety</strong>. Des mots qui marquent. Des citations pour chaque moment.</p>
  </div>
</footer>
