<div class="container py-5">
  <div class="text-center mb-5">
    <h1 class="display-4 fw-bold">💬 Explorez les catégories</h1>
    <p class="lead text-muted">Des citations inspirantes pour chaque univers.</p>
  </div>

  <% categories = [
    { slug: "motivation", name: "Motivation", description: "Trouvez l'énergie pour avancer" },
    { slug: "vie", name: "Vie", description: "Réflexions profondes sur l'existence" },
    { slug: "humour", name: "Humour", description: "Souriez avec des mots bien choisis" },
    { slug: "sagesse", name: "Sagesse", description: "L'écho des penseurs d'hier et d'aujourd'hui" },
    { slug: "litterature", name: "Littérature", description: "Les mots au service de l’art" },
    { slug: "amour", name: "Amour", description: "Des phrases qui font battre le cœur" }
  ] %>

  <div class="row row-cols-1 row-cols-sm-2 row-cols-lg-3 g-4">
    <% categories.each do |category| %>
      <div class="col">
        <%= link_to quotes_path(category: category[:slug]), class: "text-decoration-none" do %>
          <div class="card category-card shadow-sm border-0 overflow-hidden">
            <div class="category-image-wrapper">
              <%= image_tag "categories/#{category[:slug]}.jpg", class: "w-100 h-100", alt: category[:name] %>
            </div>
            <div class="category-overlay text-white text-center">
              <h5 class="fw-bold mb-1"><%= category[:name].upcase %></h5>
              <p class="small m-0"><%= category[:description] %></p>
            </div>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>
</div>

