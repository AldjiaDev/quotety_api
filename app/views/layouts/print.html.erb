<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Impression | Quotety</title>
    <style>
      @page {
        size: A4 portrait;
        margin: 2.5cm;
      }

      * {
        box-sizing: border-box;
      }

      html, body {
        margin: 0;
        padding: 0;
        font-family: 'Georgia', serif;
        background-color: #FEF9F3;
        color: #2E2E2E;
      }

      .print-container {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        height: 100vh;
        padding: 0 2rem;
        text-align: center;
      }

      .quote-text {
        font-size: 2.4rem;
        font-weight: bold;
        margin-bottom: 2rem;
      }

      .quote-author {
        font-size: 1.5rem;
        font-style: italic;
        color: #A3B18A;
      }

      .print-button {
        display: block;
        margin: 1.5rem auto 0;
        padding: 0.5rem 1.2rem;
        font-size: 1rem;
        background-color: #F4A261;
        color: white;
        border: none;
        border-radius: 6px;
        cursor: pointer;
        text-decoration: none;
      }

      .print-button:hover {
        background-color: #e26b00;
      }

      @media print {
        .print-button {
          display: none;
        }
      }
    </style>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
