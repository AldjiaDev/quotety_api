<!DOCTYPE html>
<html>
  <head>
    <title>Quotety</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta charset="utf-8">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Feuille de styles -->
    <%= stylesheet_link_tag "application", media: "all", "data-turbo-track": "reload" %>

    <!-- ✅ Bootstrap CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  </head>

  <body>
    <!-- ✅ Navbar Quotety -->
   <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm py-3 sticky-top">
  <div class="container">
    <!-- Logo -->
    <a class="navbar-brand fw-bold fs-3 text-primary" href="/">Quotety</a>

    <!-- Burger menu -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar" aria-controls="mainNavbar" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navbar content -->
    <div class="collapse navbar-collapse" id="mainNavbar">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link fw-semibold" href="/quotes">Citations</a>
        </li>
        <li class="nav-item">
          <a class="nav-link fw-semibold" href="/authors">Auteurs</a>
        </li>
        <li class="nav-item">
          <a class="nav-link fw-semibold" href="/favorites">Favoris</a>
        </li>
      </ul>

      <!-- Barre de recherche -->
      <form class="d-flex me-3" method="get" action="/quotes/search">
        <input class="form-control rounded-pill px-3 me-2" type="search" name="q" placeholder="Rechercher..." aria-label="Recherche">
      </form>

      <!-- Connexion -->
      <a href="/login" class="btn btn-outline-primary rounded-pill">Connexion</a>
    </div>
  </div>
</nav>


    <!-- Contenu de la page -->
    <%= yield %>

    <!-- ✅ Bootstrap JS (menu burger & interactivité) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
