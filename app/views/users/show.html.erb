<div class="container py-5">
  <h1 class="fw-bold mb-4 text-center">👤 Mon Profil</h1>

  <div class="card shadow-sm mx-auto" style="max-width: 500px; background-color: #fffaf3; border-radius: 1rem;">
    <div class="card-body text-center py-5">
      <h3 class="fw-bold mb-3"><%= @user.username %></h3>
      <p class="text-muted mb-4"><%= @user.email %></p>

      <%= link_to "Mes citations favorites", favorites_path, class: "btn btn-outline-primary rounded-pill me-2" %>
      <%= link_to "Déconnexion", logout_path, method: :delete, class: "btn btn-outline-danger rounded-pill" %>
    </div>
  </div>
</div>
